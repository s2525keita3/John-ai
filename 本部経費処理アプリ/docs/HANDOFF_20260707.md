# 引き継ぎメモ（2026-07-07 時点 / v1.1.0）

このメモは他のPC・別セッションで開発を続けるためのもの。
アプリ: https://john-ai-honbu-keihi-ver1.streamlit.app/ （main へ push で自動デプロイ）

## この日にやったこと

1. **摘要マッチングの正規化**（`classifier.py` の `normalize_for_match`）
   - 全角/半角・大文字小文字・スペース有無の揺れを吸収。
   - これで「GOOGLE *CHATGPT」と「GOOGLE*CHATGPT」等の不一致による判断不能が解消。
2. **判断不能クイックマスタ登録UI**（`app.py`、分類実行後に表示）
   - キーワード＋PL選択→「追加」で即再分類。恒久化は「現在のマスタをダウンロード」
     → `sample_master.csv` 差し替え、または次回「② マスタ」で上書き読込。
3. **子ども・子育て支援金(会社) 対応**（`payroll_hq.py`）
   - 2026年4月支給分からの国制度。人件費縦計に追加。
   - 旧様式（行が無い一覧表）は `OPTIONAL_ROW_LABELS` でエラーにしない。
4. **桜木町（あおぞら＋手動＋小口）モードの分類整備**
   - `sample_master.csv` に桜木町キーワードを一括追加。
   - 2026年1〜7月の実CSV（GMOあおぞら `statement-*.csv`）423行で検証し、
     除外後の全311行が「確定」・判断不能ゼロ。

## 桜木町の分類ポリシー（オーナー確認済み）

- **除外**（`aozora_filters.py`）: スタッフ給与振込（姓カナで判定）・カ）ジヨンへの
  振替（本部送金）・ATMゆうちょ（小口補充）・PE電子納付・社会保険料
  → 支給控除一覧・小口帳側で把握するため二重計上防止。
- **入金に分類**: 医療保険入金（国保連・支払基金）／リコーリース（利用者負担金の
  集金代行）／高齢福祉課／利用者個人の振込。
- **エイトプラス・イナミ シャーリーモモ = 車屋**（修理・軽自動車購入）
  → 車両運搬費。給与ではない（一度誤って除外し訂正済み。摘要の見た目で
  推測せず、不明な支払先はオーナーに確認すること）。
- リコーリースは表記で区別: 「リコ－リ－ス」（全角ハイフン・入金）と
  「リコーリース」（長音・リース料）。脆い区別なので触るとき注意。

## 開発環境の注意（このリポジトリ固有）

- ソースは OneDrive 配下。`git status` が非常に遅い（タイムアウトすることも）。
  ファイル指定で `git add` する。
- OneDrive の影響で commit が `cannot update the ref` で失敗する場合は
  `git config windows.appendAtomically false`（設定済み）。
- マスタ・コードを直したら **必ず GitHub へ push**。ローカルだけ直すと
  デプロイ側で判断不能が再発する（実際に発生した）。

## 次の候補（未着手）

- 桜木町の手動入力・小口の運用確認（小口スプシ「桜木町　小口」からの取込は手動）。
- 新子安・白根・さいわい など他店舗への横展開（桜木町と同じ型でいける見込み）。
- 「普通預金 利息」が桜木町でも本部売上に入る（微額）。気になるなら店舗別に分ける。
